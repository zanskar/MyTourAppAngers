<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>
    <!-- Style for a tab that displays a category name -->
    <style name="CategoryTab" parent="Widget.Design.TabLayout">
        <item name="tabIndicatorColor">@color/tabind</item>
        <item name="tabSelectedTextColor">@color/selected</item>
        <item name="tabTextAppearance">@style/CategoryTabTextAppearance</item>
    </style>
    <!-- Text appearance style for a category tab -->
    <style name="CategoryTabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textColor">@color/textcolor</item>
        <item name="android:textSize">@dimen/tabtextsize</item>
    </style>
    <!-- Titles styles for items -->
    <style name="titlestyle">
        <item name="android:textSize">@dimen/titletextsize</item>
        <item name="android:textColor">@color/titlecolor</item>
        <item name="android:textStyle">bold</item>
        <item name="android:layout_marginLeft">@dimen/textmargin</item>
    </style>
    <!-- Text styles for items -->
    <style name="itemstyle">
        <item name="android:textSize">@dimen/textsize</item>
        <item name="android:textColor">@color/textcolor</item>
        <item name="android:layout_marginLeft">@dimen/textmargin</item>
        <item name="android:layout_marginRight">@dimen/textmargin</item>
    </style>
</resources>